# Infrastructure

This directory will contain Docker, Docker Compose, and later deployment infrastructure such as Terraform.

Infrastructure files are kept separate so that local development and deployment configuration do not become mixed with application business logic.

The first Docker Compose configuration will be added later in Phase 1.
